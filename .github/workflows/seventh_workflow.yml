# №7 Запуск 'Workflow' по расписанию. Каждые 5 минут.
# - cron: "*/5 * * * *"
#
#

name: Seventh Workflow
on:
  schedule:
    - cron: "*/2 * * * *"

jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Event Vars
        run: |
          echo $GITHUB_SHA
          echo $GITHUB_REF
