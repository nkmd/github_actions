name: First Workflow    
on: [push]              

jobs:                   
  run-first-comand:     
    runs-on: ubuntu-latest    
    steps:                    
      - name: Display some Text
        run: echo "__Some Text__"
      - name: Command in Multeline
        run: | 
          node -v
          npm -v

